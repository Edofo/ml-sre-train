name: CI Gate

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - "app/**"
      - "infra/terraform/**"

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: No app/terraform changes
        run: echo "No app or terraform changes in this PR"
