apiVersion: v1
kind: Service
metadata:
  name: recommender
  labels:
    app.kubernetes.io/name: recommender
spec:
  type: ClusterIP
  selector:
    app: recommender
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080